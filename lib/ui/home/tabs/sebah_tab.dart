import 'package:flutter/material.dart';

class SebahTab extends StatelessWidget {
  const SebahTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.purpleAccent,);
  }
}